# Milestone 05: Quota-Aware MVP Evidence and Conventions

This directory houses the evidence writeups, deterministic admission specifications, and architectural evaluations for Milestone 05 (Quota-Aware MVP).

---

## Fixture and Redaction Conventions

Consistent with repository and global standing agent contracts (`~/.config/agents/AGENTS.md` and repository `AGENTS.md`):

1. **Format-Valid Synthetic Identifiers**:
   - RFC 9562 UUIDv7 synthetic format: `01950000-0000-7000-8000-000000000001`
   - Preserves variant nibble (`8`, `9`, `a`, `b`) and version nibble (`7` for v7, `4` for v4).
2. **Zero Credentials or Secrets**:
   - No API keys, session tokens, or credentials are committed.
3. **Zero Real Machine Paths**:
   - Generic root placeholders (`$WORKSPACE` or relative paths) are used exclusively.
4. **No Hidden Model Reasoning**:
   - Reasoning tokens and private scratchpads are never persisted.
5. **Factual Claim Labels**:
   - `VERIFIED`: Proven by committed code, test suite execution, or exact command outputs.
   - `REPO-INSPECTION`: Derived from direct inspection of committed repository files.
   - `SCHEMA-ONLY`: Derived from contract schemas without runtime integration.
   - `UNVERIFIED`: Not verified in this slice.

---

## Directory Inventory

- `admission-policy.md`: Architectural specification and evaluation contract for deterministic versioned admission decisions (`admission.decided` v1), operational reserve thresholds, manual confirmation boundaries, delayed recheck policy, and SQLite concurrency guarantees.
- `cobbler-commands.md`: Durable goal-scoped command ids with identical-replay / conflicting-reuse semantics, validated command state machine with recoverable `needs_user`, atomic intent/transition/result persistence, trajectory rebuild, SQLite-enforced exclusive global MVP task claim, and the honest limitations of this slice (execution disabled; direct run paths unprotected).
- `response-attribution.md`: Strict command-response attribution follow-up — fail-closed `confirmed_by` on every new response, the `system:` prefix convention, digest-covered persistence to row + `cobbler.command.resolved` v1 (additive), no-backfill rationale, and the fail-closed matrix.
- `execution-entry.md`: Loop-closure I1 — claim+grant dispatches only through the durable dispatch pipeline (`enqueue_for_run`: dispatch record + Oban job, never direct `start_run`), `/runs/new` as a Cobbler command submitter with a logged expert/test hatch, the production entrypoint inventory, and the lock-vs-documentation ledger.
- `terminal-checkpoint.md`: Loop-closure I3 — terminal-path recovery checkpoints with real repository evidence (new collector module + pre-commit hook in the Elf terminal region), the P1–P5 ledger with deviations, collection inventory, and the lock-vs-documentation ledger.
- `worker-effect.md`: Loop-closure W2 — production dispatch-worker effect (`Shoestring.Harness.Dispatch.ElfEffect` + prod wiring in `config/runtime.exs`): worker delivery starts the Elf with the exact start/outcome mapping, the P1–P5 ledger with deviations, and the lock-vs-documentation ledger.
- `wakeup-continuation.md`: Second-round finding 3 — admitted wakes dispatch leased same-provider continuations (projected or fallback-written recovery context, run-provider identity, per-run lease grant, claim gate, retry convergence), with the fail-on-base ledger.
- `round-3-fixes.md`: Second-review BLOCKERs R3.1–R3.7 — decline stops + quiet exit, wake supersede + persisted decisions, live worker transport, multi-cycle renewal + renewal logging, resume-first start, record-threaded handoff prompts, genuine eval arms; per-finding lock ledgers.
- `handoff-production.md`: Iteration-5 production cross-provider handoff — the `run.handoff` durable Cobbler command with its authorized decision refs, `Shoestring.Cobbler.Handoffs` (authorize/identity first, one-active-Elf guard, boundary + supersede check, fresh receiver observation + admission, receiver-owned lease grant, `handoff.created`, durable supervised dispatch, idempotent convergence), the `handoff` queue plus worker and boot reconciler that consume the intent end to end, the removal of the unsupervised `Elves.resume_run/2` cross-provider bypass, the `resolve_session/2` twin of R4.2, recovery of the decision-to-pointer crash window (a handoff must not read its own admission decision as an external change and refuse itself forever), durable `handoff.failed` settling so permanently failed intents are never resurrected by reconcile or a restart, measured baselines at four heads, and the per-round finding maps with their honest lock-vs-documentation split.
- `round-4-fixes.md`: Third-review BLOCKERs R4.1–R4.7 — snapshot-keyed wake decisions + pending-dispatch neutralize, dispatch-first cross-provider session lookup, interrupted resumability + full decline sequence, composed fallback prompts, epoch-keyed renewal evidence, leased UI manual runs, quota-interrupted fixture leg; per-finding lock ledgers.
- `goal-ui.md`: Goal-page worktree and provider explanation — real worktree identity from the durable record keyed by run id, an actively executing provider kept strictly separate from an admission candidate and from provider-reported diagnostic evidence, explicit per-state unknowns, and the fail-on-base ledger.
- `integration-closeout.md`: Aggregate integration of the three approved Iteration-5 slices (#72 checkpoint/resume, #73 handoff, #74 goal UI) onto one branch — the per-PR commit-range determination and why #72's range starts before the base, proof that the integrated tree is exactly the union of the three PR heads with no new source or test content, the measured integrated gate (`mix precommit`: 1288 tests, 0 failures, 1 skipped, 6 `:live` excluded; Node 52/0) with the targeted suite table, and an acceptance audit against the original milestone contract, recovered verbatim and now tracked at `plans/milestones/05-quota-aware-mvp.md` — the nine acceptance criteria, work packages A–G including a full item-by-item audit of all seven package-G bullets, all ten deterministic matrix rows, the semantic/ablation arms and the demo — with the unpromoted iteration-4 live-verification gap, residual risks, and the items left explicitly unmet (no real cross-provider handoff, no real semantic evidence, package G's two audited acceptance blockers — checkpoint artifacts never rendered and no next boundary rendered or computed, each with a bounded proposed fix — and UI visual validation with no reachable browser). Records that hermetic completeness is not milestone acceptance completeness, and that iteration 6 is not unlocked.
- `live-cross-provider-handoff.md`: the bounded live-verification record (2026-09-21). **It closes no acceptance gate**: §0 states which gates stay OPEN and why (the live transfer bypassed the `:prod` Observatory probe and the worker's decision step, both workarounds for open defects). Records a real Codex→Claude transfer through the durable `run.handoff` intent, real receiver observation and admission (refused without an operator confirmation, admitted with one), the receiver's own lease, the canonical pointer and the durable dispatch pipeline; real receiver semantics quoted from canonical normalized events; mechanical verification of the resulting disposable Go CLI project; an explicit owned-process cancellation with its terminal classification; the two production defects fixed here (the handoff intent had no confirmation channel, and its attribution is now derived from a trusted context rather than accepted from the request; a terminal arriving before `run.starting` wedged the goal's projector, with its cancel-path twin) each with a base-failure ledger; the hard-stop matrix proving a confirmation lifts none of them; the defects found and deliberately **not** fixed; one launch failure whose cause was **not** established; and the honest multi-run gate. Records that iteration 6 remains locked.
- `prod-handoff-lease-policy.md`: the production handoff path repair — the `:prod`-configured Observatory receiver probe produced a snapshot the work goal could not project, wedging the goal's `harness` projector permanently (`live-cross-provider-handoff.md` §7.2, previously NOT FIXED), repaired by minting a deterministic goal-local snapshot identity chained to the ledger reading **without** weakening the locked same-goal ownership rule; and the durable per-transfer `lease_policy` on the `run.handoff` intent (allow-listed, range-bounded, digest-covered, carried on the resolved result so enqueue / retry / boot repair all reach identical bounds), whose default deadline is the user-relaxed 2700 seconds rather than the wake-shaped 300 that stopped the live receiver leg mid-task (§6.3). Includes the measured base-regression ledger with its honest lock-vs-documentation split, the reason both acceptance-7 causes are removed while the gate itself stays OPEN, and the untraced wake-path twin.
- `live-production-rerun.md`: the 2026-09-23 live rerun on a `MIX_ENV=prod`-configured node (Opus 5.5 verified from the Claude `init` frame). **It closes no acceptance gate.** It records three production defects on the genuine path, none patched: no Claude observation ever reaches the configured `:handoff_observe` ledger probe, so `HandoffWorker` fails `no_observation_for_provider`; the Elf's lease-renewal boundary re-appends an Observatory-owned Codex snapshot under the work goal, wedging its projector so the canonical checkpoint is never projected and the `run.handoff` request is rejected `handoff_checkpoint_not_found`; and the trajectory writer never classifies Exqlite's `"Database busy"` as retryable, so 3 of 5 Codex launches failed before `run.starting`. It also records one completed Codex turn with its verified Go artifact, a crash-recovery redelivery that started exactly one Elf, explicit cancellation of that live Elf, and the fact that the Claude receiver, the three-arm ablation and handoff tax were **not** run or measured.
- `fixtures/live-prod-rerun/`: redacted transcripts, Go verification output and the derived summary for that rerun, produced by `tools/live_eval/export_evidence.py` (reassembled-stream, same-length redaction; covered by `live_evidence_redaction_test.exs`).
- `fixtures/live/`: the committed, redacted canonical material those claims rest on — per-run normalized event and lifecycle transcripts for all four live legs (`normalized-*.md`), the verbatim deterministic Go verification output (`go-verification.txt`), and the derived counts and boolean assertions (`cross-provider-handoff-summary.json`). Identifiers are format-valid synthetic substitutes assigned 1:1 and **same-length**, applied to the reassembled event stream rather than field by field (Codex spells a message out one delta fragment at a time, so a path can be reconstructed across events that individually match nothing — that miss, and its correction, are documented in `live-cross-provider-handoff.md` §9). `test/shoestring/evidence/live_evidence_redaction_test.exs` enforces it: it scans raw bytes plus three contiguously reassembled views for host paths, credentials, reasoning content and non-synthetic UUIDs, and checks each file's declared event counts against its actual lines.
